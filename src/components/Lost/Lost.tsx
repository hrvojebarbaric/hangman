import { text } from '../../utils/text.constatns'

const Lost = () => {
    return (
        <div>
            <h1>{text.lost.title}</h1>
            <p>{text.lost.text}</p>
        </div>
    )
}

export default Lost
