# React + TypeScript + Reduxtoolkit + Styled Components

To install a project, you can run

### `npm install`

To run app in the development mode, you can run

### `npm dev`

To build app, you can run

### `npm build`
